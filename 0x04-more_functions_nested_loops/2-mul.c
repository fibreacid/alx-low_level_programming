#include "main.h"
#include <stdio.h>

/**
 * mul -  multiplies two integers.
 *@a: multiply two integers
 *@b: multiply two integers
 *
 * Return: mul for a, mul for b
 */
int mul(int a, int b)
{
	return (a * b);
}
