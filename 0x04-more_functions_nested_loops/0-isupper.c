#include "main.h"
#include <isupper.h>
#include <stdio.h>

/**
 * _isupper - Checks for uppercase character.
 * Returns: 1 if c is uppercase, 0 otherwise.
 */

int _isupper(int c)
{
	char c;

	if (c == 'C')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
