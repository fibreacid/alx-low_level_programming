#include <stdio.h>

/**
 * main - Prints the lowercase alphabet in reverse.
 *
 * Return: 0
 */

int main(void)

{

char alphabet;

for (alphabet = 'z'; alphabet >= 'a'; letter--)
{
	 putchar(alphabet);
}
putchar('\n');
return (0);
}
